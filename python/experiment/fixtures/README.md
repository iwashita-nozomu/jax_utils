# Test Fixtures

テスト用の固定データディレクトリです。

**未実装（実装予定）**:

- `small_cases.json`
  - 小規模テストケース（d=1-3, level=1-3）
  - ユニットテストで使用

- `baseline_results.json`
  - 既知の参照結果（d=2, level=2）
  - 回帰テスト・検証に使用

**用途**:
- smoke test の高速実行
- 回帰テストの参照データ
- 他の実験での比較基準

**作成方法**:
初回実行後に生成されます。
