# 演算子記法の厳格ルール

## 要約
- 線形は `@`、合成は `*`。
- 混在は括弧で明示します。

## 規約
- **線形作用素の適用は `@` を使う**（例: `Mv @ v`）。
- **線形作用素の合成は `*` を使う**（例: `Mv * proj`）。
- 非線形作用素は `Op` を使い、**適用は `__call__`**（例: `op(x)`）。
- `@` と `*` を混在させる場合は**括弧で明示**します（例: `A @ (B @ v)`）。
- `LinOp` / `Op` の内部関数を直接呼び出さず、**演算子記法を優先**します。
