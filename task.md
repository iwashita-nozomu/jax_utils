# task

root の `task.md` は、現在の未解決タスクだけを短く追跡するための作業メモです。
古い branch 状態、完了済み計画、dated summary を残すことを禁止します。

## Current State

- root `task.md` で追跡する未解決タスクはありません。

## Rules

- 粒度はファイル単位でそろえます。
- 完了した項目は削除します。
- 仕様や判断は `documents/` に残します。
- 補助メモや provisional な観察は `notes/` に残します。
