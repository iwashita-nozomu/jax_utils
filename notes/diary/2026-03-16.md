# 2026-03-16

## Smolyak 実験整理

- 旧版 Smolyak 実験の結果を `results/functional-smolyak-scaling` へ整理し、`main` から辿れるメモを追加した。
- 旧版結果について、`float64` の安定性、低精度での相殺誤差、host 側格子構築のボトルネックを確認した。
- 旧 results worktree は Git の worktree 管理から外し、結果自体は branch 側へ残した。

## 参照資料

- `references/sparse_grid/` に sparse grid / combination technique / adaptive sparse grid の一次資料を追加した。

## 継続中

- 改訂版 Smolyak 実装は `results/functional-smolyak-scaling-tuned` で大規模実験を継続中。
- `work/smolyak-tuning-20260316` では積分器の軽量化と HLO 観察を継続している。
