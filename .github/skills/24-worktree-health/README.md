# Skill: `worktree-health` — ワークツリー健全性レビュー

このディレクトリは GitHub 側の implementation surface です。skill の説明正本は `agents/skills/worktree-health.md` に置きます。

## Default Commands

- `git worktree list`
- `git status --short`
- `bash scripts/tools/check_worktree_scopes.sh`
