# Repo-Wide Agent Discussion

このファイルは、run 固有 artifact に閉じない repo-wide な議論だけを残す場所です。

## Rules

- role 名を明記する
- decision / concern / request を分ける
- run 固有の詳細は `reports/agents/<run-id>/` を参照する
- 共有正本へ昇格した内容は `agents/` か `documents/` に統合し、このファイルには残さない
