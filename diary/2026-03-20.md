# 日記 — 2026-03-20

## 概要

Smolyak 実験基盤の最終検証と次フェーズの方針決定を実施。
JAX fork() 互換性、Worker pickle 化検証、リソース監視が完全に動作することを確認。
medium/large test 実行に向けた branch 戦略を文書化。

## 実施内容（時系列）

### 1. JAX fork() 互換性確認

- [x] spawn context factory (`jax_context.py`) 実装完了
- [x] CPU-only での動作確認：smoke test 3.1 cases/sec
- [x] spawn 常時使用（分岐削除）でコード簡略化

### 2. Worker Pickle 化検証実装

- [x] `check_picklable()` 関数追加
- [x] StandardRunner.run() で自動検証
- [x] early error detection が機能

### 3. StandardRunner API の簡略化

- [x] `use_spawn_context` パラメータ削除
- [x] spawn は常時使用（CPU/GPU 両対応）
- [x] 実験スクリプト更新

### 4. Pyright 警告 0 件達成

**修正内容**:
- 未使用 import 削除（os, asdict, jax 単体）
- SmolyakWorker の Worker protocol 完全実装
- multiprocessing type hint 対応（type: ignore）

**結果**:
```
pyright 0 errors, 0 warnings, 0 informations ✅
```

### 5. テスト実行と性能測定

- **smoke test**: 6 cases in 1.6-1.9s → **3.1-3.7 cases/sec**
- **small test**: 50 cases in 4.8s → **10.4 cases/sec**
- リソース監視: max_workers=4, host_memory=16GB 正確に反映

### 6. ブランチ戦略の方針決定

**文書化**:
- `documents/BRANCH_STRATEGY_SMOLYAK_EXPERIMENT.md` 作成
- 段階的実験 + 最後に統合の方式を採用
  - Phase 1: smoke/small（当 worktree で継続実行）
  - Phase 2: medium/large（新 results branch で本実運用）
  - Phase 3: main へ統合

### 7. 検証結果をメモとして記録

- `notes/experiments/smolyak_infrastructure_checkpoint_20260320.md` 作成
- 性能指標、技術的成果、課題を整理

## 重要な決定

### ブランチなし実験 → 段階的実験に統一

当初：「ブランチなし実験」の検討

**決定**:
1. smoke/small は検証段階（当 worktree、notes 記録）
2. medium/large は本実段階（新 results branch、生成物保管）
3. 最终的に main へ統計・考察を統合

**理由**:
- smoke/small は数秒～数十秒で高速検証
- medium/large は数分～数十分の長時間実行 → branch 分離で管理
- 問題検出時に即座に修正できる柔軟性を保つ

### Git 操作

```bash
git add -A
git commit -m "docs: Smolyak 実験方針文書化（ブランチ戦略・検証結果記録）"
# まだ push していない
```

## 次の予定

### 今すぐ（本日）

- [ ] 上記 commits を push（main と work branch）
- [ ] medium test 実行用 results branch を検討
- [ ] 必要に応じて medium test 開始

### 明日以降

- [ ] medium test を実行（新 worktree）
- [ ] リソース監視と性能測定
- [ ] 実験結果を notes へ記録
- [ ] large test の判断・実行（オプション）

## 技術的な学び

1. **spawn context の必要性**: CPU-only でも fork() 回避が重要
   - JAX import 初期化の状態分離
   - multiprocessing 管理下での一貫性確保

2. **Worker protocol の型安全性**: pickle 化検証が早期エラー検出に有効

3. **段階的実験の利点**: smoke/small で基準設定後、medium/large の本実行がスムーズ

## Status

| 項目 | Before | After |
|:---|:---:|:---:|
| 並列実行 | 順序実行（for-loop） | 4 parallel workers |
| スループット | N/A | 3.1-10.4 cases/sec |
| Pyright | 12 errors | 0 errors ✅ |
| JAX 互換性 | 不明確 | spawn 常時（確認済） |
| ブランチ戦略 | pending | 文書化完了 ✅ |
